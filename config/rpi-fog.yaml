device:
  layer: rpi-all
  hostname: raspberry
  user1: fog

image:
  layer: image-rpios
  boot_part_size: 200%
# TODO update size
  root_part_size: 16G
  name: rpi-fog

layer:
  user: rpi-user-credentials
  app: fogproject
  base: trixie-minbase
