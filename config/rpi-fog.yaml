device:
  layer: rpi-all
  hostname: raspberry
  user1: fog

image:
  layer: image-rpios
  boot_part_size: 512M
  root_part_size: 4G
  name: rpi-fog

layer:
  user: rpi-user-credentials
  app: fogproject
  base: trixie-minbase
